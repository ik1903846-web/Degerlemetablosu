{
  "name": "reeldeger",
  "version": "0.0.0",
  "private": true,
  "description": "REELDEĞER v2.2 — BIST Damodaran-tabanlı değerleme ve portföy sistemi",
  "packageManager": "pnpm@10.5.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
