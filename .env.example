# =============================================================================
# REELDEĞER — Environment Variables (Örnek)
# Bu dosya commit'lenir. Gerçek .env GITIGNORE'DA — commit edilmez.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Adım 4.5 docker-compose ile başlatılacak)
# -----------------------------------------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Production'da managed Postgres URL'iyle değiştirilecek (Railway/Supabase)
DATABASE_URL=postgresql://reeldeger:reeldeger_dev@localhost:5432/reeldeger_dev?schema=public

# -----------------------------------------------------------------------------
# PostgreSQL Credentials (docker-compose.yml tarafından okunur)
# -----------------------------------------------------------------------------
POSTGRES_USER=reeldeger
POSTGRES_PASSWORD=reeldeger_dev
POSTGRES_DB=reeldeger_dev

# -----------------------------------------------------------------------------
# Redis (caching + session store, Adım 4.5 docker-compose)
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
API_PORT=8000
WEB_PORT=3000

# -----------------------------------------------------------------------------
# NextAuth (Adım 5 / Faz 1.1 deploy aşamasında doldurulacak)
# -----------------------------------------------------------------------------
# NEXTAUTH_SECRET üretmek için: openssl rand -base64 32
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=

# -----------------------------------------------------------------------------
# Damodaran Fetcher (Faz 1.2'de kullanılacak)
# -----------------------------------------------------------------------------
# DAMODARAN_DATA_DIR=./data/damodaran
# DAMODARAN_CACHE_TTL=3600

# -----------------------------------------------------------------------------
# Stripe (Faz 5 paid tier için)
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PRICE_ID_MONTHLY=
# STRIPE_PRICE_ID_YEARLY=
