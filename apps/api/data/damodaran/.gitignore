# Damodaran raw data files — telifli, repo'da saklanmaz
# Sadece türetilmiş parameters.json commit'lenir (audit trail)

*.xlsx
*.xls
*.csv
*.tmp

# parameters.json istisna: türetilmiş özet, audit için commit'lenir
!parameters.json
!**/parameters.json
!**/.gitkeep

# Session 5.2: Hash state runtime artifact (her check'te değişir)
_hash_state.json

# Session 5.5+ (placeholder): fetch log audit trail
_fetch_log.jsonl
